import 'package:flutter/material.dart';

class ExternalLibrariesScreen extends StatelessWidget {
  const ExternalLibrariesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
